{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://raw.githubusercontent.com/AppaYiipYiip/microbox/main/assets/schema_input_contigs.json",
  "title": "microbox pipeline - params.input schema (params.input_type = 'contigs')",
  "description": "Schema for the samplesheet passed to --input when starting from an existing assembly rather than raw FASTQ (PLAN.md §6.7 multiple entry points)",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "sample": {
        "type": "string",
        "pattern": "^\\S+$",
        "errorMessage": "Sample name must be provided and cannot contain spaces"
      },
      "contigs": {
        "type": "string",
        "format": "file-path",
        "pattern": "^\\S+\\.(fa|fasta|fa\\.gz|fasta\\.gz)$",
        "errorMessage": "Path to an assembled contigs FASTA (optionally gzipped)"
      }
    },
    "required": ["sample", "contigs"]
  }
}
